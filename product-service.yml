test:
  message: "product-service settings: ----->"

spring:
 datasource:
   url: jdbc:postgresql://localhost:5432/product_db
   username: postgres
   password: postgres

 rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


management:
  tracing:
    sampling:
      probability: 1.0
  export:
    zipkin:
      endpoint: http://zipkin:9411/api/v2/spans



